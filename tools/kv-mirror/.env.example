# Kopieren nach ".env" (im selben Ordner) und den echten Token eintragen - siehe README.md,
# Abschnitt "Einrichtung". Die Datei ".env" selbst ist über .gitignore vom Commit ausgeschlossen.
CLOUDFLARE_API_TOKEN=
